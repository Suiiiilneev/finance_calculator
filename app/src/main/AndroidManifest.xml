<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dodesk.osirom">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="kipord.dodesk.osirom.DetailedActivity"></activity>
        <activity android:name="kipord.dodesk.osirom.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="kipord.dodesk.osirom.SecondActivity"></activity>

        <activity android:name="kipord.dodesk.osirom.AddTransactionActivity" />
        <activity android:name="kipord.dodesk.osirom.MainActivity">

        </activity>
    </application>

</manifest>